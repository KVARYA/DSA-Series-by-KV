class Solution {
public:
    void merge(vector<int>& A, int m, vector<int>& B, int n) {
        for(int j=0, i=m; j<n; j++, i++) {
            A[i] = B[j];
        }
        sort(A.begin(), A.end());
    }
};

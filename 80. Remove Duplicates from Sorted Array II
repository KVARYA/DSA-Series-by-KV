class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        int i =0;
        for(auto elem : nums) {
            if(i ==0 || i ==1 || nums[i-2] != elem) {
                nums[i] = elem;
                i++;
            }
        }
        return i;
    }
};
